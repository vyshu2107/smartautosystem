/* Smart Auto System — app.css
   Modern, responsive UI styling for Booking, Driver list, Admin Dashboard, Analytics.
   Author: ChatGPT (stylistic skeleton — plug into your HTML structure)
*/

/* ---------- Root theme variables ---------- */
:root{
  --bg: #0f1724;              /* page background */
  --card: #0b1220;            /* card background */
  --muted: #94a3b8;          /* muted text */
  --text: #e6eef8;           /* primary text */
  --accent: #06b6d4;         /* primary accent / CTAs (teal) */
  --accent-2: #ffb86b;       /* secondary accent (warm) */
  --danger: #ff6b6b;         /* error / surge high */
  --success: #10b981;        /* success / online */
  --glass: rgba(255,255,255,0.04);
  --glass-2: rgba(255,255,255,0.02);
  --shadow: 0 6px 18px rgba(2,6,23,0.6);
  --radius: 14px;
  --sm-radius: 8px;
  --glass-border: 1px solid rgba(255,255,255,0.04);
  --max-width: 1200px;
  --map-height: 60vh;
  --transition: 200ms cubic-bezier(.2,.9,.3,1);
  --z-toast: 9999;
  --font-sans: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
}

/* ---------- Global resets ---------- */
* { box-sizing: border-box; -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale; }
html,body,#app { height: 100%; }
body{
  margin:0;
  padding:0;
  font-family: var(--font-sans);
  background: linear-gradient(180deg,var(--bg), #071024 120%);
  color: var(--text);
  line-height:1.4;
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  font-size: 16px;
}

/* ---------- Layout containers ---------- */
.container{
  max-width: var(--max-width);
  margin: 28px auto;
  padding: 0 20px;
}

/* App shell */
.app-shell{
  display: grid;
  grid-template-columns: 280px 1fr 360px;
  gap: 20px;
  align-items: start;
  padding: 20px;
  max-width: var(--max-width);
  margin: 18px auto;
}

/* Collapse to two columns on medium screens, single column on small */
@media (max-width: 1100px){
  .app-shell{ grid-template-columns: 1fr 360px; }
}
@media (max-width: 820px){
  .app-shell{ grid-template-columns: 1fr; padding: 12px; }
}

/* ---------- Sidebar / Navigation ---------- */
.sidebar{
  background: linear-gradient(180deg,var(--card), rgba(11,18,32,0.85));
  border-radius: var(--radius);
  padding: 18px;
  box-shadow: var(--shadow);
  border: var(--glass-border);
  min-height: 360px;
}
.brand{
  display:flex;align-items:center;gap:12px;margin-bottom:12px;
}
.brand .logo{
  width:46px;height:46px;border-radius:10px;background:linear-gradient(135deg,var(--accent),var(--accent-2));
  display:flex;align-items:center;justify-content:center;font-weight:700;color:#071024;
  box-shadow: 0 6px 12px rgba(6,182,212,0.12);
}
.brand h1{ font-size:18px;margin:0;letter-spacing:0.2px }
.nav{
  margin-top:8px;
  display:flex;flex-direction:column;gap:6px;
}
.nav a{
  display:flex;align-items:center;gap:12px;padding:10px;border-radius:10px;color:var(--muted);text-decoration:none;
  transition: all var(--transition);
}
.nav a:hover{ color:var(--text); background:var(--glass); transform:translateY(-2px); }
.nav a.active{ color:var(--text); background:linear-gradient(90deg, rgba(6,182,212,0.08), rgba(255,184,107,0.04)); border-left:3px solid var(--accent); padding-left:9px; }

/* ---------- Map / Booking panel ---------- */
.map-panel{
  background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow);
  border: var(--glass-border);
}
.map-header{
  display:flex;justify-content:space-between;align-items:center;padding:14px 18px;border-bottom:1px solid rgba(255,255,255,0.02);
}
.map-title{ font-weight:600; font-size:16px; color:var(--text); }
.map-actions{ display:flex; gap:8px; align-items:center; }
.btn{
  background: linear-gradient(90deg,var(--accent), #00cfe8);
  color: #041021; padding:10px 14px; border-radius:10px; font-weight:600; border:none; cursor:pointer;
  transition: transform var(--transition), box-shadow var(--transition);
}
.btn.secondary{
  background: transparent; color:var(--text); border: 1px solid rgba(255,255,255,0.04);
}
.btn.ghost{ background: transparent; color:var(--muted); border: none; padding:8px; }

.btn:active{ transform: translateY(1px); }

/* Map container placeholder */
.map{
  height: var(--map-height);
  min-height: 340px;
  width:100%;
  background: linear-gradient(180deg,#021224, #061428);
  display:block;
  position: relative;
}

/* Floating search / booking card on map */
.map-fab{
  position:absolute; left:18px; top:18px; z-index:20;
  width:320px; max-width:calc(100% - 40px);
  background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  border-radius:12px; padding:12px; border:var(--glass-border); box-shadow: 0 12px 30px rgba(2,6,23,0.6);
}
.input-row{ display:flex; gap:8px; align-items:center; margin-bottom:8px; }
.input-row input, .input-row select{
  flex:1; background:transparent; border:1px solid rgba(255,255,255,0.03); padding:10px; border-radius:8px; color:var(--text);
}
.place-suggestion{ font-size:13px; color:var(--muted); margin-top:6px; }

/* ---------- Right panel — active rides / drivers / analytics ---------- */
.right-panel{
  background: linear-gradient(180deg,var(--card), rgba(11,18,32,0.85));
  border-radius: var(--radius);
  padding:14px;
  box-shadow: var(--shadow);
  border: var(--glass-border);
  min-height: 360px;
}
.section-title{ font-weight:700; color:var(--text); margin-bottom:8px; font-size:14px; }
.driver-list{ display:flex; flex-direction:column; gap:10px; max-height:calc(var(--map-height) - 20px); overflow:auto; padding-right:6px; }

/* Driver card */
.driver-card{
  display:flex;gap:12px;align-items:center;padding:10px;border-radius:12px;background:linear-gradient(180deg, rgba(255,255,255,0.01), transparent);
  border:1px solid rgba(255,255,255,0.02);
}
.driver-avatar{ width:56px;height:56px;border-radius:12px;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;font-weight:700;color:#071024; }
.driver-meta{ flex:1; display:flex;flex-direction:column; gap:4px; }
.driver-meta .name{ font-weight:700; color:var(--text); }
.driver-meta .sub{ font-size:13px;color:var(--muted); }
.driver-status{ display:flex; gap:8px; align-items:center; }
.status-dot{ width:10px;height:10px;border-radius:50%; background:var(--muted); box-shadow: 0 2px 6px rgba(0,0,0,0.4); }
.status-online{ background: var(--success); box-shadow: 0 6px 12px rgba(16,185,129,0.08); }

/* Accept / message buttons */
.card-cta{ display:flex; gap:8px; }
.small-btn{ padding:8px 10px; border-radius:8px; font-weight:600; border:none; cursor:pointer; background:transparent; color:var(--text); }
.small-btn.primary{ background: linear-gradient(90deg,var(--accent), #00cfe8); color:#041021; }

/* ---------- Booking list / table ---------- */
.table{
  width:100%;
  border-collapse:collapse;
  margin-top:8px;
}
.table th, .table td{
  padding:10px 8px; text-align:left; font-size:13px; color:var(--muted);
}
.table th{ color:var(--muted); font-weight:700; font-size:12px; text-transform:uppercase; letter-spacing:0.6px; }
.table tr{ border-bottom: 1px solid rgba(255,255,255,0.02); }

/* Status badges */
.badge{
  display:inline-block;padding:6px 8px;border-radius:999px;font-weight:700;font-size:12px;
  background:var(--glass); color:var(--text);
}
.badge.pending{ color:var(--accent-2); background: rgba(255,184,107,0.06); }
.badge.accepted{ color:var(--success); background: rgba(16,185,129,0.06); }
.badge.ongoing{ color:#fef3c7; background: rgba(255,223,82,0.06); }
.badge.surge{ color:#fff; background: linear-gradient(90deg,var(--danger), #ff8a8a); box-shadow: 0 6px 14px rgba(255,107,107,0.12); }

/* ---------- Surge indicator (visual) ---------- */
.surge-panel{
  display:flex;align-items:center;gap:10px;padding:10px;border-radius:12px;background:linear-gradient(90deg, rgba(255,107,107,0.06), rgba(255,184,107,0.03));
  border:1px solid rgba(255,107,107,0.06);
}
.surge-meter{
  width:120px;height:10px;border-radius:999px;background:rgba(255,255,255,0.03);overflow:hidden;
}
.surge-meter > i{ display:block;height:100%; width:40%; background: linear-gradient(90deg,var(--danger), var(--accent-2)); transition: width var(--transition); }

/* ---------- Analytics cards & charts ---------- */
.cards-grid{
  display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:8px;
}
@media (max-width:980px){ .cards-grid{ grid-template-columns:repeat(2,1fr);} }
@media (max-width:560px){ .cards-grid{ grid-template-columns:1fr;} }

.kpi-card{
  background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
  padding:12px;border-radius:12px;border:var(--glass-border); box-shadow: 0 10px 20px rgba(2,6,23,0.6);
}
.kpi-value{ font-size:20px; font-weight:800; color:var(--text); }
.kpi-label{ color:var(--muted); font-size:13px; }

/* Chart container placeholder */
.chart{
  height: 180px;
  background: linear-gradient(180deg, rgba(255,255,255,0.01), transparent);
  border-radius:10px;padding:10px;border:1px solid rgba(255,255,255,0.02);
}

/* ---------- Forms / inputs / controls ---------- */
.form{
  display:flex;flex-direction:column;gap:10px;
}
.input{
  padding:12px;border-radius:10px;background:transparent;border:1px solid rgba(255,255,255,0.03);color:var(--text);
}
.input:focus{ outline:none; box-shadow: 0 8px 20px rgba(6,182,212,0.06); border-color: rgba(6,182,212,0.14); transform: translateY(-1px); }

/* Toggle */
.toggle{
  width:46px;height:26px;background:rgba(255,255,255,0.04);border-radius:999px;display:inline-block;position:relative;cursor:pointer;
}
.toggle i{
  position:absolute; left:4px; top:3px; width:20px;height:20px;border-radius:50%; background:#fff; transition:left var(--transition);
}
.toggle.on{ background: linear-gradient(90deg,var(--accent), #00cfe8); }
.toggle.on i{ left:22px; }

/* ---------- Toast / Notifications ---------- */
.toast-wrap{
  position:fixed; right:20px; bottom:20px; display:flex;flex-direction:column; gap:10px; z-index:var(--z-toast);
}
.toast{
  min-width:260px; max-width:360px; padding:12px 14px; border-radius:10px; background: rgba(2,6,23,0.72);
  color:var(--text); box-shadow: 0 8px 26px rgba(2,6,23,0.7); border:1px solid rgba(255,255,255,0.03);
  display:flex; gap:10px; align-items:center;
  transform: translateY(12px); opacity:0; transition: transform 220ms ease, opacity 220ms ease;
}
.toast.show{ transform: translateY(0); opacity:1; }
.toast .icon{ width:40px;height:40px;border-radius:8px;background:var(--glass-2); display:flex;align-items:center;justify-content:center; }

/* ---------- Empty states ---------- */
.empty{
  padding:30px;border-radius:12px;background:linear-gradient(180deg, rgba(255,255,255,0.01), transparent); text-align:center;color:var(--muted);
}

/* ---------- Small helpers ---------- */
.row{ display:flex; gap:12px; align-items:center; }
.col{ display:flex; flex-direction:column; gap:8px; }
.muted{ color:var(--muted); }
.center{ display:flex; align-items:center; justify-content:center; }
.round{ border-radius:999px; }

/* ---------- Utility spacing ---------- */
.p-8{ padding:8px; } .p-12{ padding:12px; } .p-16{ padding:16px; }
.mt-8{ margin-top:8px; } .mt-12{ margin-top:12px; }
.gap-8{ gap:8px; }

/* ---------- Small animations ---------- */
.fade-in{ animation: fadeIn .35s ease both; }
@keyframes fadeIn{ from{opacity:0; transform: translateY(6px);} to{opacity:1; transform:none;} }

/* ---------- Accessibility ---------- */
button:focus, a:focus { outline: 3px dashed rgba(6,182,212,0.18); outline-offset:3px; }

/* ---------- Print / compact layout ---------- */
@media (max-width:420px){
  :root{ --map-height: 40vh; --max-width: 420px; }
  .brand h1{ font-size:16px; }
  .driver-avatar{ width:46px;height:46px; }
}

/* ---------- Example minor widget: route step list ---------- */
.route-steps{
  list-style:none;padding:12px;margin:0; border-radius:10px; background:linear-gradient(180deg, rgba(255,255,255,0.01), transparent); border:1px solid rgba(255,255,255,0.02);
}
.route-steps li{ display:flex; gap:10px; align-items:flex-start; padding:8px 0; border-bottom:1px dashed rgba(255,255,255,0.02); }
.route-steps li:last-child{ border-bottom:none; }
.route-steps .step-no{ width:28px; height:28px; border-radius:50%; background:rgba(255,255,255,0.02); color:var(--muted); display:flex;align-items:center;justify-content:center; font-weight:700; }

/* ---------- End of stylesheet ---------- */
